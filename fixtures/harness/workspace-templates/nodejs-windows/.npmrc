# Keep npm cache inside the workspace (TS-14 / Windows unprivileged runs).
# Copy to your project root as `.npmrc` when `npm install` hits EPERM on
# the global %AppData%\npm-cache.
cache=./.npm-cache
