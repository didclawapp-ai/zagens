# 通用代码事实核查规则

当审查报告中的发现使用未知语言/文件类型时，使用此回退规则。

## 1. 最基本的路径和行号要求
- file_path 必须能定位到具体文件
- line_number 必须为数字

## 2. 内容匹配（机械检查 — 字符串包含）
- `read_file` 读取对应行
- 行内容存在（非空）
- 从发现描述中提取具体符号名，检查该行是否包含该符号

## 3. 无法判断时
- 标注 "语言未知，无法深度核查"
- 不因此 FAIL — 标记为 PASS with caveat
