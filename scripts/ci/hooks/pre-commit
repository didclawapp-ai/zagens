#!/bin/sh
# Fast fmt gate on every commit. Install via scripts/ci/install-git-hooks.*
set -e
cd "$(git rev-parse --show-toplevel)"
export CARGO_TERM_COLOR=always
echo "pre-commit: cargo fmt --all -- --check"
cargo fmt --all -- --check
